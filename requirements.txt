# Runtime dependencies, as actually imported by the code and installed by scripts/run.sh.
# Lower bounds, not exact pins: the cluster installs from the Alliance wheelhouse, whose
# versions are fixed by the module (StdEnv/2023 + python/3.11) and cannot be chosen here.
#
# NumPy >= 2 is REQUIRED, not merely tolerated: cosinor.py ships the compat shim that lets
# CosinorPy (<= 3.1, written for NumPy 1.x) import under it. The previous pin of numpy==1.22
# contradicted that shim and matched no environment this project has ever run in.

torch>=2.0            # torch.autocast(bfloat16) in the pretraining loop; complex64 FFT in the SFD
numpy>=2.0
pandas>=2.0
scipy>=1.10           # signal.periodogram, in the cosinor baseline
scikit-learn>=1.3     # probes, splits, metrics
einops>=0.6           # rearrange/reduce in the encoder
matplotlib>=3.7       # every figure in hrd_rhythm.py (Agg backend, set by run.sh)
joblib>=1.2           # parallel cosinor fits (ships with scikit-learn)
umap-learn>=0.5       # optional: hrd_umap_label figures; the t-SNE views work without it
seaborn>=0.12         # HARD import-time dependency of CosinorPy
CosinorPy>=3.1        # the paper's cosinor engine; not in the Alliance wheelhouse, comes from PyPI
