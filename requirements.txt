torch>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
statsmodels>=0.14
seaborn>=0.12
# Yan et al. cosinor baseline. Install with --no-deps (slurm/setup_env.sh): fit_me needs only the above.
CosinorPy==3.1
